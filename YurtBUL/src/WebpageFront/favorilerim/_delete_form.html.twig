<form method="post" action="{{ path('favorilerim_delete', {'id': favorilerim.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favorilerim.id) }}">
    <button class="btn btn-danger " style="align-items: center" >Sil</button>
</form>

